# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/

# Secrets
.env

# Data — ingested locally, not committed (except the small gharchive slice)
de_ecosystem.duckdb
de_ecosystem.duckdb.wal
data/raw/gharchive/*.json.gz
!data/raw/gharchive/de-slice.json.gz
archive/

# xorq builds: added two originally: now not updated
builds

# xorq catalog:  committed as a static demo snapshot so it's browsable in the repo.
catalog

.env-example
.env*
!.env.example
